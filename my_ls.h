#ifndef MY_LS_H
#define MY_LS_H

#include <unistd.h>
#include <stdlib.h>
#include <stdio.h>

typedef struct {
    int show_all;       // -a
// add the others flags here

} ls_options_t;

// basic functions prototype

// custom functions prototype

#endif /* MY_LS_H */